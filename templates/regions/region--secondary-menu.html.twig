{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 */
#}
{{ attach_library('boulderD9_base/ucb-secondary-menu-region') }}
<div class="ucb-secondary-menu-region">
	<div class="ucb-secondary-menu-region-container{{ ucb_secondary_menu_position == 'above' ? ' container' : ''}}">
		{% if ucb_secondary_menu_default_links %}{% include "@boulderD9_base/includes/ucb-header-secondary-menu.html.twig" %}{% endif %}
		{{ content }}
	</div>
</div>