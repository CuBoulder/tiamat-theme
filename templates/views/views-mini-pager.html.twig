{#
/**
 * @file
 * Theme override for a views mini-pager.
 *
 * Available variables:
 * - heading_id: Pagination heading ID.
 * - items: List of pager items.
 *
 * @see template_preprocess_views_mini_pager()
 */
#}
{% if items.previous or items.next %}
  {{ attach_library('boulder_base/ucb-pager') }}
  <div class="container">
    <nav role="navigation" aria-labelledby="{{ heading_id }}">
      <h4 id="{{ heading_id }}" class="visually-hidden">{{ 'Pagination'|t }}</h4>
      <ul class="ucb-pager js-pager__items">
        {% if items.previous %}
          <li>
            <a href="{{ items.previous.href }}" title="{{ 'Go to previous page'|t }}" rel="prev"{{ items.previous.attributes|without('href', 'title', 'rel') }}>
              <span class="visually-hidden">{{ 'Previous page'|t }}</span>
              <span aria-hidden="true">‹ previous</span>
            </a>
          </li>
        {% endif %}
        {% if items.current %}
          <li class="ucb-pager-current">
            <span class="visually-hidden">{{ ('Page ' ~ items.current)|t }}</span>
            <span aria-hidden="true">{{ items.current }}</span>
          </li>
        {% endif %}
        {% if items.next %}
          <li>
            <a href="{{ items.next.href }}" title="{{ 'Go to next page'|t }}" rel="next"{{ items.next.attributes|without('href', 'title', 'rel') }}>
              <span class="visually-hidden">{{ 'Next page'|t }}</span>
              <span aria-hidden="true">next ›</span>
            </a>
          </li>
        {% endif %}
      </ul>
    </nav>
  </div>
{% endif %}
